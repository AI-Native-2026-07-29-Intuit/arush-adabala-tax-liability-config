# Generated by argocd-author
#   /argocd-author taxcalc-api --namespace-prefix taxcalc --strategy canary --secrets-mode eso
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: taxcalc-api-envs
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - env: dev
                  replicas: "1"
                  logLevel: DEBUG
                  host: taxcalc-dev.local
                - env: staging
                  replicas: "2"
                  logLevel: INFO
                  host: taxcalc-staging.local
                - env: prod
                  replicas: "3"
                  logLevel: WARN
                  host: taxcalc.local
          - clusters:
              selector:
                matchLabels:
                  uptimecrew.example.internal/tier: workload

  template:
    metadata:
      name: taxcalc-api-{{env}}
      labels:
        team: taxcalc
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: taxcalc
      source:
        repoURL: https://github.com/AI-Native-2026-07-29-Intuit/arush-adabala-tax-liability-config.git
        targetRevision: main
        path: overlays/{{env}}
      destination:
        server: "{{server}}"
        namespace: taxcalc-{{env}}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
          - ApplyOutOfSyncOnly=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
