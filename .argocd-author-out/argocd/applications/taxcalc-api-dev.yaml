# Generated by argocd-author
#   /argocd-author taxcalc-api --namespace-prefix taxcalc --strategy canary --secrets-mode eso
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taxcalc-api-dev
  namespace: argocd
  labels:
    team: taxcalc
  # Cascades to the Application's resources when the Application is deleted.
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: taxcalc

  source:
    repoURL: https://github.com/AI-Native-2026-07-29-Intuit/arush-adabala-tax-liability-config.git
    targetRevision: main
    path: overlays/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: taxcalc-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
