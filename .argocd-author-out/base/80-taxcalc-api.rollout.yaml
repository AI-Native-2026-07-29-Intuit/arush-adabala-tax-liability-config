# Generated by argocd-author (--strategy canary)
#
# This REPLACES base/10-taxcalc-api.deployment.yaml. A Rollout and a Deployment
# managing the same pods will fight; remove the Deployment from
# base/kustomization.yaml when adopting this file.
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: taxcalc-api
  labels:
    app.kubernetes.io/name: taxcalc-api
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: taxcalc-api
  strategy:
    canary:
      canaryService: taxcalc-api-canary
      stableService: taxcalc-api
      trafficRouting:
        nginx:
          stableIngress: taxcalc-api
      steps:
        - setWeight: 10
        - pause: { duration: 2m }
        - analysis:
            templates:
              - templateName: taxcalc-api-success-rate
        - setWeight: 50
        - pause: { duration: 5m }
        - analysis:
            templates:
              - templateName: taxcalc-api-success-rate
        - setWeight: 100
  template:
    metadata:
      labels:
        app.kubernetes.io/name: taxcalc-api
    spec:
      containers:
        - name: taxcalc-api
          image: uptimecrew/taxcalc-api:1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f
          ports:
            - { name: http, containerPort: 8080 }
          envFrom:
            - configMapRef: { name: taxcalc-api-config }
            - secretRef: { name: taxcalc-api-secrets }
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: http }
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: http }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: http }
            periodSeconds: 10
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits: { cpu: "1", memory: 1Gi }
---
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: taxcalc-api-success-rate
spec:
  metrics:
    - name: success-rate
      interval: 1m
      count: 3
      successCondition: result[0] >= 0.99
      failureLimit: 1
      provider:
        prometheus:
          address: http://prometheus-operated.observability.svc:9090
          query: |
            sum(rate(http_server_requests_seconds_count{job="taxcalc-api",status!~"5.."}[2m]))
            /
            sum(rate(http_server_requests_seconds_count{job="taxcalc-api"}[2m]))
