# Generated by argocd-author (--secrets-mode eso)
#
# Replaces the placeholder Secret. No secret material in Git: the operator
# reads AWS Secrets Manager and materialises taxcalc-api-secrets in-cluster.
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: taxcalc-aws
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        # IRSA: the ServiceAccount carries eks.amazonaws.com/role-arn.
        jwt:
          serviceAccountRef:
            name: taxcalc-api
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: taxcalc-api-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: taxcalc-aws
    kind: SecretStore
  target:
    name: taxcalc-api-secrets
    creationPolicy: Owner
  data:
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef:
        key: taxcalc/dev/db
        property: password
