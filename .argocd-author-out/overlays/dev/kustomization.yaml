# Generated by argocd-author
#   /argocd-author taxcalc-api --namespace-prefix taxcalc --strategy canary --secrets-mode eso
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: taxcalc-dev

resources:
  - ../../base

commonLabels:
  app.kubernetes.io/instance: taxcalc-api-dev

replicas:
  - name: taxcalc-api
    count: 1

# CI rewrites this tag via `kustomize edit set image`.
images:
  - name: uptimecrew/taxcalc-api
    newTag: 1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f1f

patches:
  - target: { kind: ConfigMap, name: taxcalc-api-config }
    patch: |
      - op: replace
        path: /data/SPRING_PROFILES_ACTIVE
        value: "k8s,dev"
      - op: replace
        path: /data/LOGGING_LEVEL_ROOT
        value: "DEBUG"

  - target: { kind: Ingress, name: taxcalc-api }
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: taxcalc-dev.local
